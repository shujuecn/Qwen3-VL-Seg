# Qwen3-VL-4B 舌体分割改造计划

## Summary

参考 `materials/2605.07141v1.pdf` 的 Qwen3-VL-Seg 思路，将当前 Qwen3-VL-4B finetune 工程改造成单类舌体二值分割模型。当前目标不是完整复现论文的开放世界 referring segmentation，而是在已有 `data/TongeImageDataset` 上做一个能训练、能推理、能评估的轻量版本。

论文最有价值的设计是：不要只把 VLM 特征直接上采样成 mask，而是把 bbox 当作结构先验，用它约束 query、像素融合和 mask refinement。第一版保留这个归纳偏置，但实现保持小而可跑。

## 已有数据

`data/TongeImageDataset` 当前包含：

- `origin_Image/*.png`：300 张舌象图像
- `origin_GT/*.png`：300 张二值 mask
- 图像与 mask 文件名一一对应，共 300 对
- 当前样例尺寸一致：`768x576`
- mask 非空，mask 面积比例约为 `0.0857 - 0.3344`，中位数约 `0.2188`
- 当前文件扩展名为 `.png`，实际内容为 BMP；实现中用 PIL 读取并转 RGB，避免 transformers/torchvision 按 PNG 解码失败。

数据规模较小，适合先做单类舌体分割和 smoke/小样本微调。训练计划应优先控制过拟合，并保留固定验证集。

## 目标范围

第一阶段只做：

- 单类舌体 foreground/background 分割
- 从 mask 自动计算 bbox，不额外标注 bbox
- 输出单张图片的 mask PNG 和 overlay
- 训练时冻结 Qwen3-VL 主体，默认只训练新增分割模块
- 指标使用 Dice、mIoU、Boundary F-score 或边界 IoU 中的至少 Dice/mIoU

第一阶段不做：

- 多类别舌象区域分割
- 多实例分割
- 开放世界 referring segmentation
- SA1B-ORS 数据构造
- 论文第二阶段的大规模混合训练

## 论文可借鉴点

Qwen3-VL-Seg 的关键设计包括：

- 使用 Qwen3-VL-4B 作为 backbone。
- MLLM 输出结构化 JSON，包含 `bbox_2d`、`label`、`mask` placeholder。
- 新增轻量 box-guided mask decoder，论文规模约 17M 参数。
- decoder 由四个部分组成：
  - multi-scale spatial feature injection
  - spatial-semantic query construction
  - box-guided high-resolution pixel fusion
  - iterative mask-aware query refinement
- 训练 loss 为文本生成 loss 加分割 loss；分割 loss 使用 BCE + Dice。
- 论文第一阶段用 LoRA、vision encoder 和 mask decoder 训练；第二阶段混合分割、通用多模态理解和推理数据。

本项目的轻量改造只吸收其中对舌体分割有直接收益的部分：

- bbox 从 GT mask 自动生成，作为训练时稳定的结构先验。
- prompt 和 assistant 输出仍使用 JSON + `<mask_start><mask_token><mask_end>`，保持与论文范式一致。
- mask decoder 使用 bbox gate 融合浅层/像素特征，避免只依赖低分辨率视觉 token。
- refinement 作为第二步增强，不阻塞第一版跑通。

## 数据转换

新增一个脚本，例如 `qwen-vl-finetune/tools/build_tongue_seg_json.py`，把 `data/TongeImageDataset` 转成 Qwen SFT 可读的 JSON：

```json
{
  "image": "origin_Image/1.png",
  "mask": "origin_GT/1.png",
  "bbox_2d": [223, 39, 568, 351],
  "label": "tongue",
  "conversations": [
    {
      "from": "human",
      "value": "<image>\nLocate and segment the tongue, report bbox coordinates and mask in JSON format."
    },
    {
      "from": "gpt",
      "value": "[{\"bbox_2d\": [223, 39, 568, 351], \"label\": \"tongue\", \"mask\": \"<mask_start><mask_token><mask_end>\"}]"
    }
  ]
}
```

建议输出：

- `data/TongeImageDataset/train.json`
- `data/TongeImageDataset/val.json`
- `data/TongeImageDataset/test.json`

默认划分：

- train: 240
- val: 30
- test: 30

转换规则：

- mask 用 `>0` 二值化。
- bbox 从 mask 非零像素计算，格式为原图像素坐标 `[x1, y1, x2, y2]`。
- 若 mask 为空直接报错，不做静默跳过。
- 保留 `mask` 字段，供 dataset 读取分割监督。
- `data_path` 注册为 `data/TongeImageDataset`，image/mask 均使用相对路径。
- 原始图像和 mask 目录不进入 Git；`train.json`、`val.json`、`test.json` 可以提交。

## 代码改造点

### 1. 数据注册

修改 `qwen-vl-finetune/qwenvl/data/__init__.py`，新增：

```python
TONGUE_SEG = {
    "annotation_path": "data/TongeImageDataset/train.json",
    "data_path": "data/TongeImageDataset",
}
```

并加入 `data_dict`。验证集和测试集建议先由独立评估脚本读取，不强行塞进当前 Trainer。

### 2. 数据读取与 collator

修改 `qwen-vl-finetune/qwenvl/data/data_processor.py`：

- `LazySupervisedDataset._get_item()` 读取 `mask` 和 `bbox_2d`。
- mask resize 到固定训练尺寸，例如 `256x256`。
- 返回：
  - `gt_masks`: `[1, H, W]` float tensor
  - `gt_boxes`: `[4]` float tensor，建议归一化到 `[0, 1]`
  - `orig_size`: `[H, W]`
- `DataCollatorForSupervisedDataset` 发现分割字段时 stack：
  - `gt_masks`: `[B, 1, H, W]`
  - `gt_boxes`: `[B, 4]`
  - `orig_size`: `[B, 2]`

第一版不要启用 `data_flatten` 或 `data_packing`，避免 packed sequence 和分割监督对齐复杂化。

### 3. 训练参数

修改 `qwen-vl-finetune/qwenvl/train/argument.py`，新增少量参数：

```python
seg_enable: bool = False
seg_mask_size: int = 256
seg_loss_weight: float = 1.0
seg_use_box_gate: bool = True
seg_refine: bool = False
```

保持参数少，先不要加入大量 decoder 配置。

### 4. 模型封装

新增 `qwen-vl-finetune/qwenvl/model/qwen3vl_seg.py`。

第一版结构：

- 内部持有 `Qwen3VLForConditionalGeneration`
- forward 在 `no_grad` 下调用 base model 和 `get_image_features(...)`，不训练 Qwen3-VL 主体
- 从 Qwen3-VL 的视觉输出或 image token hidden states 得到 dense visual feature
- 使用 `image_grid_thw` 还原视觉 token 网格
- 将 `gt_boxes` rasterize 成 soft box gate
- `TongueMaskHead` 输出 mask logits
- 返回：
  - `loss`
  - `lm_loss`，当前 Phase 1 为 None
  - `seg_loss`
  - `pred_masks`

轻量 mask head 建议：

```text
visual tokens -> 1x1 projection -> 2D grid
box Fourier/MLP embedding -> query bias
box soft gate -> feature gating
Conv + PixelShuffle/Upsample -> 1-channel logits
```

第一版可以暂时不用完整 transformer decoder，但至少保留：

- bbox gate
- BCE + Dice
- 视觉 token 2D reshape
- mask logits upsample 到 `seg_mask_size`

第二版再补：

- spatial-semantic query construction
- shallow image stem + box-guided high-resolution pixel fusion
- first-pass mask + mask-aware query refinement
- IoU confidence head

### 5. 训练入口

修改 `qwen-vl-finetune/qwenvl/train/train_qwen.py`：

- 当 `--seg_enable True` 时加载 `Qwen3VLSegForConditionalGeneration`。
- 默认冻结 base Qwen3-VL。
- 只让 mask head 和必要 adapter `requires_grad=True`。
- Phase 1 暂不支持 `--lora_enable True`。
- 保存 checkpoint 时只保存新增 `mask_head.*` 权重，不保存 frozen Qwen base，避免 safetensors 共享权重错误和大文件 checkpoint。

推荐第一阶段启动参数：

```bash
conda run --no-capture-output -n torch python qwen-vl-finetune/qwenvl/train/train_qwen.py \
  --model_name_or_path /home/zyzd/.cache/modelscope/hub/models/Qwen/Qwen3-VL-4B-Instruct \
  --dataset_use tongue_seg \
  --seg_enable True \
  --seg_mask_size 256 \
  --seg_loss_weight 1.0 \
  --bf16 True \
  --per_device_train_batch_size 1 \
  --gradient_accumulation_steps 8 \
  --learning_rate 1e-4 \
  --num_train_epochs 30 \
  --output_dir outputs/tongue_seg_head
```

## Loss

训练总 loss：

```text
loss = seg_loss_weight * seg_loss
seg_loss = BCEWithLogitsLoss(mask_logits, gt_masks) + DiceLoss(mask_logits, gt_masks)
```

注意：

- Phase 1 只训练 mask head，不计算 LM loss；assistant JSON 保留为后续扩展接口。
- DiceLoss 内部对 logits 做 sigmoid。
- mask resize 使用 nearest，图像特征 resize 使用 bilinear。

## 评估与推理

新增 `qwen-vl-finetune/tools/eval_tongue_seg.py`：

- 读取 `val.json` 或 `test.json`
- 输出 Dice、mIoU
- 保存若干 overlay 到 `outputs/tongue_seg_eval/overlays`

新增 `infer_seg.py`：

- 输入单张图片
- 自动构造 prompt：`<image>\nLocate and segment the tongue, report bbox coordinates and mask in JSON format.`
- 输出：
  - mask PNG
  - overlay PNG
  - 可选 bbox 可视化

推理阶段有两种路径：

- 简化路径：直接使用模型预测的 mask。
- 论文式路径：让 Qwen3-VL 先输出 bbox，再用 bbox-guided decoder 预测 mask。

当前舌体任务建议先用训练时 GT bbox 学到稳定 mask head；推理阶段如果 base Qwen bbox 不稳定，可以先用固定 prompt 的 bbox 输出做辅助，后续再加 bbox 评估。

## 分阶段实施

### Phase 0: 数据准备

- 生成 train/val/test JSON。
- 检查 300 对 image/mask 是否完全匹配。
- 检查每个 mask 非空，bbox 在图像范围内。
- 抽样保存 overlay，人工确认 mask 方向和图像方向一致。

### Phase 1: 最小可训练版本

- dataset 返回 `gt_masks`、`gt_boxes`。
- collator stack 分割字段。
- 新增最小 `TongueMaskHead`。
- 冻结 Qwen3-VL，只训练 mask head。
- 跑 1 batch forward/backward。
- 跑 10 step smoke test，无 NaN/OOM。

验收标准：

- `pred_masks` shape 为 `[B, 1, 256, 256]`
- `seg_loss` 正常下降或至少稳定
- 只有 mask head 参数有梯度
- checkpoint 可保存，且 `model.safetensors` 只包含 `mask_head.*` 权重
- 已验证本地 Qwen3-VL-4B-Instruct 10 step smoke test 可完成并保存 checkpoint
- 已验证 30 epoch 长训练命令可启动并正常训练

### Phase 2: 论文关键组件增强

- 加入 bbox Fourier encoding 到 query/feature bias。
- 加入 soft box gate，bbox 扩大 15%，sigmoid gate 系数初始用 20。
- 加入 shallow image stem，融合高分辨率纹理。
- 加入 first-pass mask pooling 和第二次 refinement。

验收标准：

- val Dice/mIoU 高于 Phase 1。
- overlay 边界更贴合舌体轮廓。
- 没有明显把嘴唇、牙齿、背景误分为舌体。

### Phase 3: LoRA 微调

如果 Phase 2 仍明显欠拟合，再开启 LoRA：

- target modules: `q_proj,k_proj,v_proj,o_proj`
- `lora_r=32`
- 学习率仍以 mask head 为主，LoRA 可使用更小 lr
- vision encoder 默认保持冻结；只有在 mask 边界长期不足时再考虑极小 lr 解冻视觉模块

### Phase 4: 正式评估

- 在 test split 上报告 Dice/mIoU。
- 输出 20 张代表性 overlay。
- 记录失败案例：
  - 舌尖缺失
  - 舌边界过度膨胀
  - 嘴唇/牙齿误分
  - 光照异常
  - 舌体面积过小或过大

## 风险与处理

- 数据只有 300 张，容易过拟合：固定 test split，不用 test 调参。
- 单类任务不需要完整开放世界 decoder：先实现 bbox gate + mask head，避免大改模型。
- Qwen3-VL 内部特征接口可能不稳定：优先从当前 transformers 模型可直接返回的 hidden states / visual tokens 取特征，必要时再 patch forward。
- packed data 与分割字段对齐复杂：第一版禁用 `data_flatten` 和 `data_packing`。
- 如果 24G OOM：减小 `max_pixels`、`seg_mask_size=192`、batch size 固定 1、只训练 mask head。

## 最小验收清单

- `build_tongue_seg_json.py` 能生成 train/val/test JSON。
- `dataset_use=tongue_seg` 能正常加载 240 条训练样本。
- 单 batch 包含 `input_ids`、`pixel_values`、`image_grid_thw`、`gt_masks`、`gt_boxes`。
- 10 step smoke test 通过。
- checkpoint 保存通过，且不包含 Qwen base 权重、原始图片、训练输出大文件。
- checkpoint 加载后能对单图输出 mask 和 overlay。
- test split 输出 Dice/mIoU。
